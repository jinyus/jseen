name: jseen
description: Json Tree Viewer.
version: 1.0.0
homepage: https://github.com/jinyus/jseen

environment:
    sdk: ">=3.1.5 <4.0.0"
    flutter: ">=1.17.0"

dependencies:
    flutter:
        sdk: flutter
    flutter_easy_treeview: ^1.0.6

dev_dependencies:
    flutter_lints: ^3.0.1
    flutter_test:
        sdk: flutter
